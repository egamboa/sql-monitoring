#
# List the Databases and its status
#
SELECT NAME, OPEN_MODE, CONTROLFILE_TYPE FROM V$DATABASE;

#
#
#

ALTER DATABASE CLOSE;

SELECT OPEN_MODE FROM V$DATABASE;

ALTER DATABASE DISMOUNT;

SELECT OPEN_MODE FROM V$DATABASE;
